---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: secretsyncs.sync.example.com
spec:
  group: sync.example.com
  names:
    kind: SecretSync
    listKind: SecretSyncList
    plural: secretsyncs
    singular: secretsync
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecretSync is the Schema for the secretsyncs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecretSyncSpec defines the desired state of SecretSync.
            properties:
              sourceName:
                description: sourceName is the name of the source Secret to sync.
                minLength: 1
                type: string
              sourceNamespace:
                description: sourceNamespace is the namespace of the source secret
                minLength: 1
                type: string
              targetNamespaces:
                description: targetNamespaces is a list of namespaces where the source
                  Secret should be copied to
                items:
                  type: string
                minItems: 1
                type: array
            required:
            - sourceName
            - sourceNamespace
            - targetNamespaces
            type: object
          status:
            description: SecretSyncStatus defines the observed state of SecretSync.
            properties:
              lastSyncTime:
                description: lastSyncTime is the last time the sync operation was
                  performed.
                format: date-time
                type: string
              message:
                description: message provides additional information about the sync
                  status.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
